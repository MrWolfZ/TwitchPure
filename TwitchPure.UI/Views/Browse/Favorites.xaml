<Page x:Class="TwitchPure.UI.Views.Browse.Favorites"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:TwitchPure.UI.Controls"
      xmlns:browse="using:TwitchPure.UI.ViewModels.Browse"
      xmlns:mvvm="using:Prism.Windows.Mvvm"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance browse:FavoritesViewModel}"
      mvvm:ViewModelLocator.AutoWireViewModel="True">


  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <controls:Shell DataContext="{Binding ShellViewModel}">
      <TextBlock Text="Favorites" Margin="12,12,0,0" Style="{StaticResource BaseTextBlockStyle}" />
    </controls:Shell>
  </Grid>
</Page>